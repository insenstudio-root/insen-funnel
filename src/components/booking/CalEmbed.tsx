"use client";
/**
 * CalEmbed (PRD §5.3) : embed Cal.com inline, thème aligné tokens, préchargement,
 * passage des UTM en métadonnées du booking. Fallback lien direct si l'embed échoue.
 */
export function CalEmbed() {
  // TODO: @calcom/embed-react (Cal + getCalApi) avec NEXT_PUBLIC_CALCOM_LINK + metadata UTM.
  return <div>{/* TODO embed Cal.com + fallback <a href={NEXT_PUBLIC_CALCOM_LINK}> */}</div>;
}
