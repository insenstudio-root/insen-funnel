/**
 * LeadTimeline (PRD §5.3) : liste chronologique des `lead_events`, icône par type, payload replié.
 */
export function LeadTimeline() {
  // TODO: props events[] ; rendu <ol> chronologique + icône par event_type + payload dépliable.
  return <ol>{/* TODO événements */}</ol>;
}
