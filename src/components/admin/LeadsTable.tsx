/**
 * LeadsTable (PRD §5.3) : tableau dense (desktop) — nom, société, secteur, statut (badge),
 * source (badge UTM ou "direct"), date. Filtres en query params. Curseur simple (pas de pagination sophistiquée v1).
 */
export function LeadsTable() {
  // TODO: props leads[] ; rendu table + badges statut/source ; liens vers /admin/leads/[id].
  return <table>{/* TODO en-têtes + lignes */}</table>;
}
