/**
 * Emails Resend (PRD §5.4). API server-side uniquement.
 */
// TODO: notifyInsen(lead) — alerte interne à NOTIFICATION_EMAIL à chaque lead / booking.
// TODO: confirmToProspect(lead) — email de confirmation sobre au prospect
//        (récap + à quoi s'attendre au call). Ton charte INSEN.
export {};
