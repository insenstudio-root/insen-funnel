/**
 * Client Supabase NAVIGATEUR (anon key). Lecture publique uniquement.
 * ⚠️ JAMAIS d'insert lead ici — toute écriture passe par une route API (PRD §5.6).
 */
// TODO: createBrowserClient (@supabase/ssr) avec
//   NEXT_PUBLIC_SUPABASE_URL + NEXT_PUBLIC_SUPABASE_ANON_KEY.
export {};
