/**
 * Client Supabase SERVER-SIDE (session utilisateur, via cookies).
 * Utilisé par les pages/layout `/admin` pour l'auth (PRD §4.3).
 */
// TODO: createServerClient (@supabase/ssr) avec cookies() de next/headers.
export {};
