/**
 * /consultation — cadrage du call + embed Cal.com (PRD §4.3, §5.3).
 * Récupère les UTM de l'URL et les passe en métadonnées Cal.com.
 */
export default function ConsultationPage() {
  return (
    <main>
      <h1>Consultation</h1>
      {/* TODO: cadrage (ce que c'est / ce qu'on en retire / ce que ce n'est pas) + <CalEmbed /> */}
    </main>
  );
}
