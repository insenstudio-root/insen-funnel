/**
 * /merci — confirmation. Variante selon provenance (?src=projet | booking). PRD §4.3.
 */
export default function MerciPage() {
  return (
    <main>
      <h1>Merci</h1>
      {/* TODO: message + next steps selon la provenance (formulaire ou booking) */}
    </main>
  );
}
