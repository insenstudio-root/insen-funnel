/**
 * /projet — formulaire qualifiant (PRD §4.3, §5.3). Anti-spam honeypot + rate limit.
 * Rend <ProjectForm> qui POST vers /api/leads.
 */
export default function ProjetPage() {
  return (
    <main>
      <h1>Parlez-nous de votre projet</h1>
      {/* TODO: <ProjectForm /> (5 champs qualif + 3 identité, validation Zod inline, honeypot) */}
    </main>
  );
}
