/**
 * /admin — liste des leads : filtres statut/canal/date, tri, badge source UTM. PRD §4.3.
 */
export default function AdminPage() {
  return (
    <main>
      <h1>Leads</h1>
      {/* TODO: <LeadsTable /> — lecture server-side via lib/supabase/server (RLS authenticated) */}
    </main>
  );
}
